Add const modifiers to variables, making them un-writable.
Add while loops.

# CLASSES:

* Add the ability to use `this`, both read and modify internal data.
* Add @abstract modifier, requiring that classes extending them include the method's definition.

